# Glyph Dashboard\n\nStatic, responsive dashboard for monitoring OpenClaw **sub-agents**, **sessions**, and **cron jobs**.\n\nLive JSON fetching + auto-poll every **30s**.\n\n## 🚀 Quick Deploy (GitHub Pages)\n\n1. Settings → Pages → **Source: main** (root `/`)\n2. Visit: `https://YOURUSERNAME.github.io/glyph-dashboard`\n\n✅ **Zero config, instant.** (5 mins setup)\n\n## 🔧 Setup OpenClaw API\n\n**Edit `index.html` → `API_BASE`:**\n```js\nconst API_BASE = &#39;https://your-ngrok-or-public-ip:18789/api/&#39;;\n```\n\n**Expose Gateway:**\n```bash\nopenclaw gateway restart --bind 0.0.0.0  # Public bind\n# Or ngrok http 18789\n```\n\n**Endpoints (proxy CLI if no native API):**\n- `GET /sessions_list` → `{sessions: [...]} like \`openclaw sessions list --json\``\n- `GET /cron` → `{jobs: [...]} like \`openclaw cron list --json\``\n\n*CORS:* Use nginx/Cloudflare proxy or browser extension.\n\n## 📱 Features\n\n- **Responsive UI** (table → cards on mobile)\n- Live **sub-agent filtering** + status (running/active/idle by age)\n- **Runtime**, tokens %, model\n- **Zero deps** (vanilla JS/HTML/CSS)\n- Dark gradient theme, smooth hovers\n\n## 🧪 Local Preview\n\n```bash\n# Python 3\npython3 -m http.server 8080\n# Open http://localhost:8080/index.html\n```\n\n**Note:** Local fetch blocked by CORS → Use proxy or `chrome --disable-web-security`.\n\n## 🔄 Cron Example (Auto-update proxy JSON)\n\n```bash\n# In OpenClaw cron: every 30s → write JSON to public file → dashboard polls\nopenclaw cron add &#39;* * * * *&#39; &#39;sessions_list &gt; /public/sessions.json &amp;&amp; cron_list &gt; /public/cron.json&#39;\n```\n\n## GitHub\n\nPush to `main` → auto-deploys.\n\n💼 Built for Glyph Johnson by OpenClaw subagent.